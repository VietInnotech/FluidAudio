# FluidAudio ASR Server — environment variables
# Copy this file to .env and fill in your values.

# ── Server ────────────────────────────────────────────────────────────────────

# Address the server binds to. Use 0.0.0.0 to accept external connections.
FLUIDAUDIO_SERVER_HOST=127.0.0.1

# TCP port.
FLUIDAUDIO_SERVER_PORT=8080

# Bearer token required on all /v1/* requests.
# Leave unset to disable authentication (useful for local-only dev).
# FLUIDAUDIO_SERVER_API_KEY=change-me

# Maximum multipart upload size in megabytes.
FLUIDAUDIO_SERVER_MAX_UPLOAD_MB=128

# Maximum audio duration accepted, in seconds.
FLUIDAUDIO_SERVER_MAX_AUDIO_SECONDS=900

# ── Models ────────────────────────────────────────────────────────────────────

# Root directory where CoreML models are cached / loaded from.
# Defaults to ~/Library/Application Support/FluidAudio/Models
# FLUIDAUDIO_MODELS_DIR=/Users/you/offasr/FluidAudio/Models
